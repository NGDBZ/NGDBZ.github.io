不需要使用 pip 单独安装，直接通过 import json 就可以在代码中使用它。

dump：数据从「内存」→「外部存储」（“倒出去”）；把字典 / 列表等，转成 JSON 格式
load：数据从「外部存储」→「内存」（“装进来”）。把JSON 格式数据，转回字典 / 列表等 Python 对象
两者是 “倒出去” 和 “装进来” 的反向操作，刚好对应序列化和反序列化，这个命名也符合英文的动作逻辑（比如 “dump cargo” 是卸货物，“load cargo” 是装货物）。